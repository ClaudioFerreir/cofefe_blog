import styled from 'styled-components'

import { colors } from '../../styles'

export const ButtonContainer = styled.button`
  background-color: ${colors.extraLightBrown};
  color: ${colors.darkBrown};
  border-radius: 20px;
  padding: 8px 12px;
  border: none;
  font-weight: 700;
  font-size: 20px;
`
